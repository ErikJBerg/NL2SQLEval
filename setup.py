from setuptools import setup, find_packages

setup(
    name='nl2sqleval',
    version='0.1.0',
    description='A library for evaluating and comparing SQL queries generated from natural language queries.',
    author='Your Name',
    author_email='your.email@example.com',
    packages=find_packages(),
    install_requires=[
        # List your library's dependencies here
    ],
)